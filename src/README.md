# Source Code Map

- `src/graph/` Graph model, extraction, and traversal store
- `src/retrieval/` Retrieval modes (graph, vector, hybrid)
- `src/pipeline/` CLI entrypoints (`ingest`, `query`, `evaluate`)
- `src/llm/` LLM answer generation adapter (replace placeholder)
- `src/common/` Shared config utilities
