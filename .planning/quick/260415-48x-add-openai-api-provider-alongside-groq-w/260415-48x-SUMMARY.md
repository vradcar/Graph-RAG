---
phase: quick
plan: 260415-48x
subsystem: llm
tags: [provider-factory, openai, groq, instructor, refactor]
dependency_graph:
  requires: []
  provides: [src/llm/provider.py]
  affects: [src/ingest/entity_extractor.py, config/settings.yaml]
tech_stack:
  added: [openai>=1.30.0]
  patterns: [provider factory, instructor structured output]
key_files:
  created: [src/llm/provider.py]
  modified: [src/ingest/entity_extractor.py, config/settings.yaml, requirements.txt]
decisions:
  - "Provider factory uses lazy imports (from groq import Groq inside the if-branch) to avoid import errors when the other provider's package is not installed"
  - "Groq client uses instructor.Mode.TOOLS; OpenAI client uses default tool mode (no explicit mode needed)"
metrics:
  duration: ~5min
  completed: 2026-04-15
---

# Quick Task 260415-48x: Add OpenAI API Provider Alongside Groq Summary

**One-liner:** Provider factory module `src/llm/provider.py` unifies Groq and OpenAI under a single `build_instructor_client(provider)` call, switchable via `llm.provider` in `settings.yaml` with no code changes.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create unified LLM provider factory | c815c82 | src/llm/provider.py, config/settings.yaml, requirements.txt |
| 2 | Refactor entity_extractor to use provider factory | bef8609 | src/ingest/entity_extractor.py |

## Verification

- `python -c "from src.llm.provider import build_instructor_client, get_llm_config; print(get_llm_config())"` → `{'provider': 'groq', 'model': 'moonshotai/kimi-k2-instruct-0905'}`
- `python -c "from src.ingest.entity_extractor import build_client; print('OK')"` → `OK`
- `grep -r 'from groq import' src/` → only `src/llm/provider.py` (not entity_extractor.py)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- src/llm/provider.py: FOUND
- commit c815c82: FOUND
- commit bef8609: FOUND
