---
phase: 01-schema-provenance-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pytest.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_schema.py
  - tests/test_provenance.py
  - src/graph/schema.py
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
tags: [neo4j, schema, provenance, testing]

must_haves:
  truths:
    - "Document dataclass exists in src/graph/schema.py with required fields (doc_id, title, sku, source_url, ingested_at)"
    - "VALID_KINDS includes 'Document'; VALID_RELATIONS includes 'MENTIONED_IN'"
    - "pytest is installed and runnable; integration marker registered"
    - "Failing test suite encodes all SCHEMA-01..05 + idempotency expectations and is committed RED"
    - "Existing EntityNode / RelationEdge / ALLOWED_RELATIONS members are unchanged (additive only)"
  artifacts:
    - path: "src/graph/schema.py"
      provides: "Document dataclass; Document in NODE_KIND; MENTIONED_IN in ALLOWED_RELATIONS"
      contains: "class Document"
    - path: "tests/conftest.py"
      provides: "neo4j_driver and clean_db fixtures with skip-if-unreachable"
      contains: "pytest.skip"
    - path: "tests/test_schema.py"
      provides: "Unit tests for Document validation, NODE_KIND/VALID_RELATIONS membership"
    - path: "tests/test_provenance.py"
      provides: "Integration tests covering SCHEMA-01..05 + idempotent re-ingest"
    - path: "pytest.ini"
      provides: "pytest config with integration marker registration"
    - path: "requirements.txt"
      provides: "pytest>=8.0 added"
      contains: "pytest"
  key_links:
    - from: "tests/conftest.py"
      to: "NEO4J_URI / NEO4J_USER / NEO4J_PASSWORD"
      via: "python-dotenv load_dotenv() + GraphDatabase.driver().verify_connectivity()"
      pattern: "verify_connectivity"
    - from: "src/graph/schema.py"
      to: "Existing EntityNode validation"
      via: "Adding 'Document' to NODE_KIND Literal so EntityNode(kind='Document', ...) becomes valid"
      pattern: "NODE_KIND = Literal"
---

<objective>
Establish the additive schema vocabulary (Document node kind, MENTIONED_IN relation) and lay down a failing pytest suite that encodes every Phase 1 success criterion. Tests will go GREEN in Plan 02 once the loader is refactored. This is the contract-first / interface-first task that downstream plans implement against.

Purpose: A failing test suite makes SCHEMA-01..05 executable, not just prose. Schema additions are isolated from loader logic so they can land safely without touching write paths.

Output:
- src/graph/schema.py extended additively (Document, MENTIONED_IN)
- tests/ scaffolding ready for Plan 02 to turn green
- pytest installed, configured, integration marker registered
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-schema-provenance-foundation/01-RESEARCH.md
@CLAUDE.md
@src/graph/schema.py
@src/graph/neo4j_loader.py
@src/graph/store.py
@requirements.txt

<interfaces>
<!-- Existing schema.py contract — DO NOT BREAK. Additions only. -->

From src/graph/schema.py (current):
```python
NODE_KIND = Literal["Product", "Accessory", "WiringConfig", "HVACSystemType", "Spec"]
ALLOWED_RELATIONS = Literal["COMPATIBLE_WITH", "REPLACES", "SUPPORTS_WIRING", "HAS_SPEC"]
VALID_KINDS: set[str] = set(get_args(NODE_KIND))
VALID_RELATIONS: set[str] = set(get_args(ALLOWED_RELATIONS))

@dataclass
class EntityNode:
    node_id: str
    label: str
    kind: str
    properties: Dict[str, Any] = field(default_factory=dict)

@dataclass
class RelationEdge:
    source_id: str
    target_id: str
    relation: str
    properties: Dict[str, Any] = field(default_factory=dict)
```

Target additions (additive only):
```python
NODE_KIND = Literal["Product", "Accessory", "WiringConfig", "HVACSystemType", "Spec", "Document"]
ALLOWED_RELATIONS = Literal["COMPATIBLE_WITH", "REPLACES", "SUPPORTS_WIRING", "HAS_SPEC", "MENTIONED_IN"]

@dataclass
class Document:
    doc_id: str           # canonical manifest slug, e.g. "t9_install_guide"
    title: str
    sku: Optional[str] = None
    source_url: Optional[str] = None
    ingested_at: Optional[str] = None  # ISO-8601; DB stores as datetime()
```
</interfaces>

<locked_decisions>
- D-01: doc_id is a manifest slug string (e.g., "t9_install_guide"), NOT a content hash. Phase 2 owns the manifest format; Phase 1 only fixes the contract.
- D-02: T9 backfill is by `--reset` re-ingest (handled in Plan 03), not by a Cypher migration.
- D-03: Phase 1 stops at the loader. ingest.py / pdf_parser / entity_extractor are NOT touched. Loader gets a `--doc-id` CLI flag (Plan 02).
- D-04: MENTIONED_IN carries only `first_mentioned` timestamp in Phase 1. Page numbers / mention counts deferred.
- D-05: Test infra: live local Neo4j with `pytest.mark.integration`; tests skip if NEO4J_URI is unreachable. No testcontainers.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Document dataclass + MENTIONED_IN to schema.py (additive)</name>
  <files>src/graph/schema.py</files>
  <behavior>
    - EntityNode(kind="Document", node_id="x", label="x") no longer raises (Document in VALID_KINDS).
    - RelationEdge(source_id="a", target_id="b", relation="MENTIONED_IN") no longer raises.
    - All previously-valid kinds and relations still validate (no regression).
    - Document(doc_id="t9_install_guide", title="T9 Install Guide") instantiates cleanly.
    - Document with missing required field (doc_id or title) raises TypeError at construction.
  </behavior>
  <action>
    Edit src/graph/schema.py additively per D-01 and the interfaces block above:
    1. Extend `NODE_KIND` Literal to append `"Document"`.
    2. Extend `ALLOWED_RELATIONS` Literal to append `"MENTIONED_IN"`.
    3. Add `from typing import Optional` import.
    4. Append a `Document` dataclass: required `doc_id: str`, `title: str`; optional `sku: Optional[str] = None`, `source_url: Optional[str] = None`, `ingested_at: Optional[str] = None`. No `__post_init__` validation beyond dataclass defaults.
    5. Do NOT modify EntityNode, RelationEdge, or VALID_KINDS / VALID_RELATIONS derivation (they re-derive from the Literal via get_args).

    Why this matters: schema.py is the single source of truth Plan 02's provenance.py and neo4j_loader.py will import from. Strictly additive keeps T9 ingestion non-regressing (Phase 2 INGEST-05 dependency).
  </action>
  <verify>
    <automated>python -c "from src.graph.schema import Document, VALID_KINDS, VALID_RELATIONS, EntityNode, RelationEdge; assert 'Document' in VALID_KINDS; assert 'MENTIONED_IN' in VALID_RELATIONS; EntityNode(node_id='d1', label='Doc', kind='Document'); RelationEdge(source_id='a', target_id='b', relation='MENTIONED_IN'); RelationEdge(source_id='a', target_id='b', relation='COMPATIBLE_WITH'); Document(doc_id='t9', title='T9'); print('OK')"</automated>
  </verify>
  <done>Schema additions land; existing kinds/relations untouched; Document dataclass instantiates with required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Install pytest + create test scaffolding (conftest, pytest.ini, requirements)</name>
  <files>requirements.txt, pytest.ini, tests/__init__.py, tests/conftest.py</files>
  <action>
    1. Append `pytest>=8.0` to requirements.txt (do not reorder existing pins).
    2. Create `pytest.ini` registering the `integration` marker and pointing testpaths at `tests/`:
       ```
       [pytest]
       testpaths = tests
       markers =
           integration: requires a live Neo4j (skips if NEO4J_URI unreachable)
       ```
    3. Create empty `tests/__init__.py`.
    4. Create `tests/conftest.py` with these fixtures (per D-05):
       - `neo4j_driver` (session-scoped): loads `.env`, builds `GraphDatabase.driver(uri, auth=(user, password))`, calls `verify_connectivity()`. On `ServiceUnavailable` or missing `NEO4J_PASSWORD`, calls `pytest.skip("Neo4j not reachable", allow_module_level=False)` so individual integration tests skip cleanly.
       - `clean_db` (function-scoped, depends on neo4j_driver): runs `MATCH (n) DETACH DELETE n` before yielding; teardown is a no-op (next test cleans).
       - `sample_doc_a` and `sample_doc_b`: two dicts shaped `{"doc_id": "doc_a", "title": "Doc A", "sku": "SKU-A", "source_url": "http://a", "ingested_at": "2026-05-02T00:00:00Z"}` and equivalent for B.

    Install pytest locally so the suite is runnable: `pip install -r requirements.txt` (developer step, but verify install in this task).
  </action>
  <verify>
    <automated>pip install pytest>=8.0 >/dev/null 2>&1 && pytest --version && pytest --collect-only tests/ 2>&1 | head -20</automated>
  </verify>
  <done>pytest>=8.0 in requirements.txt; pytest.ini registers integration marker; conftest.py provides driver+clean_db+sample doc fixtures with skip-if-unreachable behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing test suites encoding SCHEMA-01..05 + idempotency</name>
  <files>tests/test_schema.py, tests/test_provenance.py</files>
  <behavior>
    All tests below MUST exist and be discoverable. Unit tests in test_schema.py pass NOW (after Task 1). Integration tests in test_provenance.py FAIL NOW (helpers don't exist yet) — Plan 02 turns them green.

    Unit tests (test_schema.py — pass after Task 1):
    - test_document_required_fields: Document(doc_id="x", title="y") works; Document() raises TypeError.
    - test_document_kind_in_valid_kinds: "Document" in VALID_KINDS.
    - test_mentioned_in_in_valid_relations: "MENTIONED_IN" in VALID_RELATIONS.
    - test_existing_kinds_preserved: {"Product","Accessory","WiringConfig","HVACSystemType","Spec"} ⊆ VALID_KINDS.
    - test_existing_relations_preserved: {"COMPATIBLE_WITH","REPLACES","SUPPORTS_WIRING","HAS_SPEC"} ⊆ VALID_RELATIONS.

    Integration tests (test_provenance.py — marked @pytest.mark.integration, will FAIL until Plan 02):
    - test_document_upsert: merge_document writes :Document with all 5 props; re-running updates `last_reingested` but keeps `first_ingested`.
    - test_mentioned_in_created: merge_node_with_provenance creates (n)-[:MENTIONED_IN]->(:Document); re-run yields exactly 1 MENTIONED_IN edge.
    - test_no_null_source_doc: after ingesting a fact, audit query `MATCH ()-[r]->() WHERE type(r) <> 'MENTIONED_IN' AND r.source_doc IS NULL RETURN count(r)` returns 0.
    - test_parallel_evidence_edges: ingesting same (source, target, relation) from doc_a and doc_b yields exactly 2 parallel edges with `source_doc` values {"doc_a","doc_b"} (SCHEMA-04).
    - test_node_dedup_with_source_docs: ingesting node "T9" from doc_a then doc_b yields exactly 1 :Product node with `source_docs == ["doc_a","doc_b"]` (sorted) (SCHEMA-05).
    - test_idempotent_reingest: ingesting same node from same doc twice yields exactly 1 node with `source_docs == ["doc_a"]` (length 1, NOT 2 — coll.distinct guard).
    - test_idempotent_edge_reingest: ingesting same edge from same doc twice yields exactly 1 edge.
    - test_mentioned_in_count_matches_source_docs: for every node with `source_docs`, `size(source_docs) == count((n)-[:MENTIONED_IN]->())`.
  </behavior>
  <action>
    Implement both test files per the behavior contract above.

    For test_provenance.py:
    - Import the not-yet-existing helpers: `from src.graph.provenance import merge_document, merge_node_with_provenance, merge_edge_with_provenance`. Plan 02 creates this module. Tests will collect-error or fail on ImportError until then — that's the RED state.
    - Use the `clean_db` and `neo4j_driver` fixtures.
    - Each test opens a session via `neo4j_driver.session()` and calls `session.execute_write(lambda tx: merge_document(tx, sample_doc_a))` etc.
    - Audit assertions use `session.run(...).single()` patterns from RESEARCH.md "Cypher audit queries".

    Do NOT skip the integration tests for missing helpers — let them fail loudly so Plan 02's progress is visible via the test counter.
  </action>
  <verify>
    <automated>pytest tests/test_schema.py -x -q 2>&1 | tail -5 && pytest tests/test_provenance.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_schema.py: 5/5 unit tests PASS.
    test_provenance.py: 8 tests collected, all currently failing/erroring on missing src.graph.provenance import. This is the expected RED baseline for Plan 02 to flip GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → Neo4j | Local-only Bolt connection; credentials from .env; no untrusted input. |
| Filesystem (.env) → process env | Standard dotenv load; no new secrets introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tests/conftest.py `clean_db` fixture | accept | Test fixture intentionally wipes the local dev DB. Risk is operator running tests against a non-test instance. Mitigation: skip-if-unreachable + dev runs against localhost:7687 only; document NOT to point NEO4J_URI at production. |
| T-01-02 | Information Disclosure | NEO4J_PASSWORD in .env | accept | Existing project pattern; .env is gitignored; no Phase 1 change. |
| T-01-03 | Denial of Service | Neo4j connection storm during pytest | accept | Session-scoped driver fixture reuses a single driver across the whole test session. |
</threat_model>

<verification>
1. `python -c "from src.graph.schema import Document, VALID_KINDS, VALID_RELATIONS"` succeeds.
2. `pytest tests/test_schema.py -x` → 5 passed.
3. `pytest tests/test_provenance.py --collect-only` → 8 collected (failures on import are OK at this stage).
4. `git diff src/graph/schema.py` shows ONLY additions (no lines removed from EntityNode/RelationEdge/existing literals).
</verification>

<success_criteria>
- Document dataclass + MENTIONED_IN literal landed additively in schema.py
- 5 unit tests passing in test_schema.py
- 8 integration tests collected (RED) in test_provenance.py
- pytest>=8.0 in requirements.txt; pytest.ini registers integration marker
- conftest.py provides skip-if-unreachable Neo4j fixtures
- Plan 02 has a stable contract to implement against (provenance helper signatures)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-provenance-foundation/01-01-SUMMARY.md` documenting:
- Schema additions (exact lines)
- Helper-function signatures Plan 02 must implement (from test_provenance.py imports)
- Current test counter: 5 GREEN unit, 8 RED integration
</output>
